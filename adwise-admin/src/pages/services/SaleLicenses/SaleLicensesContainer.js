// @flow
import { compose } from 'recompose';
import { connect } from 'react-redux';

import SaleLicenses from './SaleLicenses';

export default compose(
  connect(
    state => ({}),
    dispatch => ({}),
  ),
)(SaleLicenses);
