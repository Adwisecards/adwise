import React from "react";
import {
    Box,
    Grid,
    Typography
} from "@material-ui/core";

const DisabledLocalStorage = () => {
    return (
        <Box>
            <Typography variant="h1"></Typography>
        </Box>
    )
}

export default DisabledLocalStorage
