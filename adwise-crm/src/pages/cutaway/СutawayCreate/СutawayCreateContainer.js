// @flow
import { compose } from 'recompose';
import { connect } from 'react-redux';

import СutawayCreate from './СutawayCreate';

export default compose(
  connect(
    state => ({}),
    dispatch => ({}),
  ),
)(СutawayCreate);
