import React from 'react';
import {
    View,
    StyleSheet
} from 'react-native';
import Contact from '../Contact';

const ListContact = (props) => {
    const { list, onActiveUser, activeUser } = props;

    return (
        <View style={styles.root}>
        </View>
    )
}

const styles = StyleSheet.create({
   root: {

   }
});

export default ListContact
