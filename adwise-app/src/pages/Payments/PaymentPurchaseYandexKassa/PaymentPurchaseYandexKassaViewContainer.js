// @flow
import { compose } from 'recompose';
import { connect } from 'react-redux';

import PaymentPurchaseYandexKassaView from './PaymentPurchaseYandexKassaView';

export default compose(
  connect(
    state => ({}),
    dispatch => ({}),
  ),
)(PaymentPurchaseYandexKassaView);
