// @flow
import { compose } from 'recompose';
import { connect } from 'react-redux';

import ResetPasswordPhoneView from './ResetPasswordPhoneView';

export default compose(
  connect(
    state => ({}),
    dispatch => ({}),
  ),
)(ResetPasswordPhoneView);
