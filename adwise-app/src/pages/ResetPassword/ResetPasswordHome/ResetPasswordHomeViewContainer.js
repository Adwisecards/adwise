// @flow
import { compose } from 'recompose';
import { connect } from 'react-redux';

import ResetPasswordHomeView from './ResetPasswordHomeView';

export default compose(
  connect(
    state => ({}),
    dispatch => ({}),
  ),
)(ResetPasswordHomeView);
