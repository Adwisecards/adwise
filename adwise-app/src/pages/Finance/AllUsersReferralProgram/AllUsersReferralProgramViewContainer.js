// @flow
import { compose } from 'recompose';
import { connect } from 'react-redux';

import AllUsersReferralProgramView from './AllUsersReferralProgramView';

export default compose(
  connect(
    state => ({}),
    dispatch => ({}),
  ),
)(AllUsersReferralProgramView);
