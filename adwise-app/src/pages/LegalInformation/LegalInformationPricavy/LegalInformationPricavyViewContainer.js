// @flow
import { compose } from 'recompose';
import { connect } from 'react-redux';

import LegalInformationPricavyView from './LegalInformationPricavyView';

export default compose(
  connect(
    state => ({}),
    dispatch => ({}),
  ),
)(LegalInformationPricavyView);
