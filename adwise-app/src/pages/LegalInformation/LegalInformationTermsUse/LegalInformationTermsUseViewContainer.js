// @flow
import { compose } from 'recompose';
import { connect } from 'react-redux';

import LegalInformationTermsUseView from './LegalInformationTermsUseView';

export default compose(
  connect(
    state => ({}),
    dispatch => ({}),
  ),
)(LegalInformationTermsUseView);
